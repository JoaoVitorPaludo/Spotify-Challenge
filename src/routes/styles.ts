import styled from 'styled-components'

export const PrivateRoutesContainer = styled.div`
  display: flex;
  height: 100vh;

  @media (max-width: 768px) {
    display: flex;
    flex-direction: column;
  }
`
